
// This file is kept for backward compatibility
// All functionality is now in the direct-markers directory

import { createDirectMarkers } from './direct-markers';

// Re-export the main function for backward compatibility
export { createDirectMarkers };
